This is scripts for probably copying into a ~/scripts dir in your home folder, that help with using latex


Just run `./copy-to-home` and it should work, including overwriting the `.latexmkrc` in your home dir. 
